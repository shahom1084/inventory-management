[build]
  base = "frontend"
  command = "npm install && npm run build"
  publish = "dist"

[[redirects]]
  from = "/api/*"
  to = "https://inventory-management-rg9u.onrender.com/api/:splat"
  status = 200
  force = true